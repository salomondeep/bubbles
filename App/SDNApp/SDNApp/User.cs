namespace SDNApp
{
    internal class User
    {
        public string name;
        public string id;

        public override string ToString()
        {
            return "Name: " + name + "     " + "Id:" + id;
        }
    }
}